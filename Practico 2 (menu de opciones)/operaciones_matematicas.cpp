#include "stdafx.h"
#include "operaciones_matematicas.h"

int square(int x) {
	return SQUARE(x);
}

int sum(int a, int b) {
	return SUM(a, b);
}

double area(double r) {
	return AREA(r);
}

int max(int x, int y) {
	return MAX(x, y);
}